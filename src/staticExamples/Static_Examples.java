package staticExamples;

public class Static_Examples {

	public static void main(String[] args) {
		
		double pow = Math.pow(4, 2);
		System.out.println(pow);
		
		// Static variables are class level variables
		// Static belongs t the class and not to the objects
		// Static call - to call a static member, you have to name 
		// the class. name of the member
		
		// Class Math is a class that comes with Java, it has functions to perform 
		// mathmatical operations
		
		// Static means constant and not changable
		// Dynamic means changeable 
		
		

	}

}
